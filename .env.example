# Percent move from previous close that triggers an alert (default 13)
THRESHOLD_PCT=13

# Liquidity screen (prior-session NSE bhavcopy)
MIN_TURNOVER_CR=25
MIN_PRICE=20

# Feed: mock (no credentials) or kite
FEED_MODE=mock

# Zerodha Kite Connect (required when FEED_MODE=kite)
# Paid Connect plan (~₹500/month) is required for live WebSocket market data.
KITE_API_KEY=
KITE_ACCESS_TOKEN=

# Telegram Bot API (optional — falls back to console if unset)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional: path to a newline-separated ticker file (overrides liquidity screen)
# CUSTOM_UNIVERSE_FILE=

# Where alert dedupe state is stored
STATE_DIR=.nse_alert
